## 1. Implementation

- [x] 1.1 Add `initSubmoduleBranches` helper (detached only; switch -c / switch; no commit)
- [x] 1.2 Wire `StartOptions.initSubmoduleBranches` + CLI `--init-submodule-branches`
- [x] 1.3 Extend `StartResult` with `submoduleBranches` + warnings on failure
- [x] 1.4 Unit tests with mock git runner
- [x] 1.5 ops-start skill + README note; help text

## 2. Closeout

- [ ] 2.1 Ship/merge; comment/close issue #2 when done
