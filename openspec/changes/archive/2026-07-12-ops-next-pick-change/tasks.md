## 1. Discovery + handler

- [x] 1.1 Add `listCandidateChanges(roots)` (worktrees leaf + active openspec/changes; unit tests)
- [x] 1.2 Extension `/ops-next`: no name → discover → 0/1/N pick → existing action menu
- [x] 1.3 Headless multi: text list only, no auto-pick
- [x] 1.4 ops-next skill + brief README note

## 2. Verify

- [x] 2.1 Tests green; `/ops-next <name>` behavior unchanged
