## 1. Extension + parse helper

- [x] 1.1 Add `parseSlashChangeAndRest` (or reuse firstKebab + rest) with unit test
- [x] 1.2 `registerCommand("ops-deliver")`: bind name / pick candidates / followUp skill message
- [x] 1.3 Update ops-deliver SKILL.md to prefer extension-bound name

## 2. Docs

- [x] 2.1 Prompt + README one-liner: slash args handled by extension

## 3. Verify

- [x] 3.1 Tests green; no full pipeline reimplementation in extension
